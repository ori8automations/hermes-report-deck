# Plain note without frontmatter

This report has no YAML frontmatter at all. Report Deck still lists it: the
title comes from the first heading, the id is derived from the filename, and
the other metadata fields are simply empty.

- Frontmatter is optional
- Any `.md` or `.markdown` file under the report root shows up
- Metadata-based filters (lane, source, tag, date) just won't match this one
